app:
  photo_dir: /photos
  port: 5050

paths:
  cache_dir: /cache
  photo_cache_dir: /cache/photos
  log_dir: /logs

cache:
  limit: 2000
  same_day_cycle: 100
image:
  max_width: 2080
  max_height: 768
